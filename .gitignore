# OS
.DS_Store

# Node
node_modules/
dist/
out/
*.log

# Local env / secrets (keys belong in the OS keychain, never here)
.env
.env.*

# SQLite runtime data
*.sqlite
*.sqlite-*
*.db

# Research run artifacts (charted from the CSVs, not committed)
/research/

# Throwaway probes
/dump_mem.mts
